# Local dev: Vite proxy forwards /api and /ws to the backend (see vite.config.ts).
# Production (Vercel): set these in the project Environment Variables.
# VITE_API_BASE_URL=https://your-backend.example.com/api
# VITE_WS_BASE_URL=wss://your-backend.example.com/ws
